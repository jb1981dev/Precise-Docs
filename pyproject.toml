[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "precisetools"
authors = [{name = "PreciseTools", email = "precise-support@proton.me"}]
dynamic = ["version", "description"]
